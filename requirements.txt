# Thu vien cho nhanh yolo-plc (GUI PyQt5: camera MindVision + YOLO + PLC S7)
# Cai vao venv:  .venv\Scripts\python.exe -m pip install -r requirements.txt

# --- Vision / YOLO ---
ultralytics==8.4.83          # YOLO11 (train + detect)
torch==2.12.1                # backend (CPU; doi sang ban CUDA neu co GPU)
torchvision==0.27.1
opencv-python-headless==4.13.0.92   # xu ly anh (hien thi bang PyQt5)
numpy==2.5.0
pillow==12.2.0               # camera_mv chuyen doi anh (Image.convert)

# --- Giao dien ---
PyQt5==5.15.11               # GUI vision_serial_gui.py

# --- PLC Siemens S7 ---
python-snap7==3.0.0          # ket noi PLC S7-1200/1500 (kem snap7.dll cho Windows)

# --- Ghi chu ---
# - SDK camera MindVision (mvsdk) nam trong mindvision_python/mvsdk.py (khong qua pip),
#   can driver/DLL MindVision cai san tren may.
# - ultralytics keo theo opencv-python (ban day du) nen may se co ca 2 ban opencv;
#   import cv2 van chay binh thuong.
# - Co GPU NVIDIA: cai torch ban CUDA tu https://pytorch.org thay cho dong torch o tren.
